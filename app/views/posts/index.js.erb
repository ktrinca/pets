$('#more-post').append('<%= j render "posts" %>');
<% if @posts.next_page %>
  $('.infinite-scrolling').replaceWith('<%= j will_paginate @posts %>');
  $('.pagination').remove();
<% else %>
  $('.infinite-scrolling').addClass('submitted');
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>